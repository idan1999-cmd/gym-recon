services:
  - type: web
    name: ariel-fit-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 dashboard/backend/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.6
